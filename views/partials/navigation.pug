nav.navigation 
    .navigation__wrapper
      a.navigation__link(href="/")=meta.data.title
      svg.navigation__link__icon
       ul.navigation__list 
        each item in navigation.data.list
          li.navigation_list__item 
            a.navigation__list__link(href=(item.link.slug))=item.text